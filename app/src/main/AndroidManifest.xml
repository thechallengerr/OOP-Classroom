<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hus.oop_classroom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
        <activity android:name=".RVcourse"></activity>
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:theme="@style/Theme.OOPClassroom.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgotPassword" />
        <activity android:name=".Home" />
        <activity android:name=".Navigation.UserProfile" />
        <activity android:name=".Register" />
        <activity android:name=".Navigation.Communication.Share" />
        <activity android:name=".Navigation.Communication.AppRating" />
        <activity android:name=".Navigation.Personal.Course.Nav_Course" />
        <activity android:name=".Navigation.Personal.Lectures" />
        <activity android:name=".Navigation.Personal.Events" />
        <activity android:name=".Navigation.Personal.Announcements" />
    </application>

</manifest>