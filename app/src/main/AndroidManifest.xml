<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hus.oop_classroom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
        <activity android:name=".TeachersHome"></activity>
        <activity android:name=".RVcourse" />
        <activity
            android:name=".Activity.Login"
            android:label="@string/app_name"
            android:theme="@style/Theme.OOPClassroom.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.ForgotPassword" />
        <activity android:name=".Home" />
        <activity android:name=".Navigation.UserProfile" />
        <activity android:name=".Activity.Register" />
        <activity android:name=".Navigation.Communication.ActivityShare" />
        <activity android:name=".Navigation.Communication.ActivityAppRating" />
        <activity android:name=".Navigation.Personal.Course.Nav_Course" />
        <activity android:name=".model.Lectures" />
        <activity android:name=".model.Events" />
        <activity android:name=".model.Announcements" />
    </application>

</manifest>